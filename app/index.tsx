// This file exists as a placeholder for expo-router.
// The actual app routing is handled by AppNavigator via the _layout.tsx.
// Expo-router requires at least one route file in the app/ directory.

export default function Index() {
    // The _layout.tsx handles all navigation via AppNavigator
    return null;
}
